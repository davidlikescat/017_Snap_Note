{
  "installCommand": "npm --prefix ./api install && npm --prefix ./frontend install",
  "buildCommand": "npm --prefix ./frontend run build",
  "outputDirectory": "frontend/dist",
  "functions": {
    "api/**/*.ts": {
      "runtime": "nodejs20.x"
    }
  }
}
